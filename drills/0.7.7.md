# 0.7.7 — drill record

Written 2026-08-27 by `cndgrr` against the 0.7.7 release PR (Refs #510),
candidate branch `build/510-release-0-7-7` on `heavy-duty/ceremony`, cut from
`main` at `4a22bc2` — the merge of PR #511.

No disposable-repo rehearsal was executed for this cut and none is owed; this
file is a scope ruling, not an instrument emission. `drill/rehearsal.sh` was
not invoked, no scratch repo was created, and there is therefore no scratch
repo to name and no disposal to observe.

## Scope ruling — doors unchanged, no disposable-repo rehearsal

The three conditions in [drills/README.md](README.md#doors-unchanged) hold at
this candidate head, and each was measured here rather than copied from an
earlier record or from the authorizing issue (#233). The authorizing issue
says so twice, in its own words: the record is a measurement, never an
inheritance (#510 D2).

**1. The release-path diff is the pin line and nothing else.** The baseline is
`0.7.6`, and the measurement over the release path, taken at the candidate
commit that carries the stamp, the assembled section and the four pins, is:

```
$ git diff --numstat 0.7.6 -- <release-path>
1	1	.github/workflows/release.yml

$ git diff -U0 0.7.6 -- <release-path> | grep -E '^[-+]' | grep -vE '^(\+\+\+|---)'
-  CEREMONY_SELF_REF: "0.7.6"
+  CEREMONY_SELF_REF: "0.7.7"
```

One file, one line, and that line is the `CEREMONY_SELF_REF` pin the condition
exempts by name. No door logic, no decide table, no facts gathering, no tag
classifier, no changelog extractor and no `bin/` script differs from the last
rehearsed tag.

This record's own commit adds `drills/0.7.7.md` and nothing else, and
`drills/` is not on the release path, so the reading above is the reading at
the head that carries this file too. Re-run both commands to confirm it; that
is what they are printed for.

Measured at the branch point, `main` at `4a22bc2` — before this PR's own stamp
— the same diff is **empty**: `git diff --numstat 0.7.6 -- <release-path>`
prints nothing. Nothing on the release path had moved since the `0.7.6` tag at
all, and this candidate's entire release-path delta is the pin stamp it made
itself.

The gating issue this cut waited on, #506, is the reason that emptiness is
worth stating rather than assuming: its change is to
`.github/workflows/labels-sweep.yml`, which is not on the release path, so it
could not have moved this diff — and the measurement above is what establishes
that, not the reasoning.

**2. The release path came from the script, not from transcription.**
`.github/scripts/release-path.sh` returned **seven** entries at this head, and
the diff above was taken over exactly them:

```
.github/workflows/release.yml
bin/
lib/tag-classify.sh
lib/version.sh
lib/decide.sh
lib/facts.sh
lib/changelog.sh
```

`lib/tag-classify.sh` is the seventh, and it is newer than the prose:
[drills/README.md](README.md#doors-unchanged)'s condition 2 still enumerates
the six the path held before the tag door's classifier became a file of its
own. The normative half of that condition is *exactly the output of the
script*, which is what was run here, and `test/release-path.test.sh` is what
keeps the script true — it reds both a missing dependency and a stale path. The
inline list is the condition's copy-paste convenience and it is one entry
behind; that is a documentation drift reported rather than repaired here, this
PR being the cut and not its fix.

**3. The baseline is the last rehearsed tag, and it qualifies.**
[drills/0.7.6.md](0.7.6.md) is a **full rehearsal** — it carries the
instrument's own run sentence as its third line, naming
`build/499-release-0-7-6` and canonical candidate SHA `cb7a061`, and all
**eleven** probes ran, eleven rows in its table, every one of them passing.
The `0.7.6` release is published (`2026-08-26T14:07:58Z`, `draft=false`,
`prerelease=false`, target `main`), and `main` was re-armed to `0.7.7-dev`
afterwards at `046a839` (`2026-08-26T14:07:59Z`, "chore: bump main to
0.7.7-dev — re-arm after publishing 0.7.6", a one-line `VERSION` change).

Here the last **rehearsed** tag is also the **previous** tag. That is a fact
about this cut and not the rule: the anchor is the last rehearsed tag (#233),
and because this record is itself doors-unchanged it anchors nothing — `0.7.6`
remains the baseline for `0.7.8`, and every release-path change accumulated
between now and then measures against it rather than against `0.7.7`.

**4. The four `CEREMONY_SELF_REF` carriers, three of them off the release
path.** The pin check requires every workflow carrying the env to name one
release and reds the tree otherwise, so this cut stamps four files while the
measurement above sees one. Counted at this candidate head by the guard's own
scan rather than carried from an earlier cut (#510 D5), the carriers are
`.github/workflows/release.yml:160` (the one on the release path, and the line
the diff shows), `.github/workflows/ci-rerun.yml:43`,
`.github/workflows/labels.yml:58` and
`.github/workflows/labels-sweep.yml:109`. Each moved by exactly one line,
`"0.7.6"` to `"0.7.7"`. They are named here so the candidate's full diff and
this record cannot be read as disagreeing.

The count is four and has been four since `ci-rerun.yml` joined before the
`0.7.4` cut; it is stated as a measurement anyway, because the two previous
cuts that inherited it are exactly why #510 D5 asks for it afresh. Stamping
three of four reds `.github/scripts/self-ref-check.sh` at this head, which is
the guard doing its job (#351).

**A waiver was considered and is the weaker record.** `WAIVED` is a
maintainer's judgement under the standing paragraph; `doors unchanged` is a
mechanically checked claim the panel can re-run from this file. Where the
conditions hold, the checkable shape is the one owed — the narrower shape is
not a substitute for a rehearsal or a waiver, but it is not optional in favour
of them either.

## Standing evidence at the candidate head

| # | probe | where | result |
|---|---|---|---|
| 1 | decide + merge-door step-replay (dogfood) | `release-exercise / step-replay (dogfood)` on this PR | CI-gating; green required to merge |
| 2 | decide + merge-door step-replay (consumer) | `release-exercise / step-replay (consumer)` on this PR | CI-gating; green required to merge |
| 3 | fragment chain: armed → assemble → monotonic | `release-exercise / fixture-chain` on this PR | CI-gating; green required to merge |
| 4 | the real tree's own guards (armed, monotonic, assembled, drill-recorded, runner-isolated, sha-pinned) | `self-guards` on this PR | CI-gating; green required to merge |
| 5 | the drill record's round trip, classifying this file rather than grading it | `self-guards / Drill record round trip` on this PR | CI-gating; green required to merge |
| 6 | the self-ref pin: every workflow names one ceremony release | `test / Self-ref pin` on this PR | CI-gating; green required to merge |
| 7 | the documentation availability markers against the assembled section | `test / Documentation availability markers` on this PR | CI-gating; green required to merge |
| 8 | the contract suite, shellcheck and actionlint | `test / Tests`, `test / Shellcheck`, `test / Actionlint` on this PR | CI-gating; green required to merge |

Probe 6 covers the pin measurement this record's fourth block is about: a
stale or partial `CEREMONY_SELF_REF` fails ceremony's own CI here, so all four
stamps are verified by the same run that verifies everything else. Probe 5 is
the one to read rather than tick — its log says which branch it took, and the
branch this record expects is the one that classifies a scope ruling and
declines the round trip (#373 D9).

## Deviations

None. This record stages no scratch caller, so nothing needs to resolve
`CEREMONY_SELF_REF: "0.7.7"` before the tag exists — the candidate-ref
deviation that shape would raise does not arise.

The payload is four fragments: #501, #505, #506 and #508. Three of them are
one story told from three sides — a taxonomy bootstrap that was evicted from a
shared concurrency queue, one that could not report whether it had been
delivered, and one that died on an over-long `operator` description and
abandoned every row after it. They land in `actions/labels-reconcile/`, in
`.github/workflows/labels-sweep.yml`, in `.github/labels.conf`, in
`lib/checks.sh` and `actions/issueflow-reconcile/`, and in
`docs/CONSUMERS.md`. None of it is on the release path and no release door
reads any of it: the bootstrap job is a labels-side path, the reconciler and
its libs are consumed by the labels workflows, and the doctrine and consumer
docs are bytes a governed repo re-vendors at its next pin bump. No
release-workflow input, no new release guard, and no change to any door a
drill would probe.

This cut carries **no availability-marker clearance**, and that is a
measurement rather than an omission. `**unreleased**` was scanned for
wrap-tolerantly and case-insensitively over every tracked file at this
candidate — not only the `*.md` set `marker-check.sh` itself walks — and the
one live occurrence is `docs/CONSUMERS.md:308`, inside an inline-code span. It
is the convention's own definition of the token, the guard strips inline code
before scanning, and #510 D4 names it as the site that must not be touched. No
marker anywhere in the tree cites #501, #505, #506 or #508, so the assembled
section makes none of them false and none moves.

The assembled section does **not** open with the "makes a consumer's first
bootstrap trustworthy" sentence #510's Context asks for, and the omission is
deliberate. `actions/changelog-assembled` asserts that a release PR's stamped
section is byte-for-byte what its consumed fragments assemble to, replaying
`changelog-assemble --check` over the merge base's fragment set — the guard
that exists precisely so nobody hand-edits assembled prose. A lead sentence
typed into that section would red it. The story is told in the release PR body
and in this record instead; putting it in the published notes would need a
fifth fragment, which is a mint and not a builder's call.
