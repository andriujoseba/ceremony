# 0.6.1 — drill record

Run 2026-08-06 by `andriujoseba` against the 0.6.1 release PR (Refs #333),
candidate branch `build/333-release-0-6-1`.

## Scope ruling — doors unchanged, no disposable-repo rehearsal

All three conditions in `drills/README.md` were measured at this candidate
head. The release path is byte-identical to the last rehearsed tag, `0.6.0`,
except for the permitted `CEREMONY_SELF_REF` pin stamp. A new disposable-repo
rehearsal would therefore execute the same door bytes as 0.6.0. The panel
reviews this claim like any other evidence; if any reviewer rules a full drill
owed, that verdict wins.

## Measurements at the candidate head

1. The stricter baseline diff is empty apart from the permitted pin stamp:

   ```console
   $ git diff 0.6.0..HEAD -- $(.github/scripts/release-path.sh)
    .github/workflows/release.yml | 2 +-
    1 file changed, 1 insertion(+), 1 deletion(-)
   ```

   Inspection of that one hunk shows only `CEREMONY_SELF_REF: "0.6.0"`
   becoming `CEREMONY_SELF_REF: "0.6.1"`. No release-door logic or release
   script changed.

2. The release path was taken from the candidate's script rather than
   transcribed:

   ```console
   $ .github/scripts/release-path.sh
   .github/workflows/release.yml
   bin/
   lib/version.sh
   lib/decide.sh
   lib/facts.sh
   lib/changelog.sh
   ```

3. `0.6.0` is the last rehearsed tag. Its own `drills/0.6.0.md` records a
   full six-probe disposable-repo rehearsal, the release is published at
   `https://github.com/heavy-duty/ceremony/releases/tag/0.6.0`, and commit
   `8c3a4d1` re-armed `main` to `0.6.1-dev` immediately after publication.

## Standing evidence at the candidate head

The release PR's gating checks replay both decision paths, the merge-door
step sequence, the fragment chain, and the real tree's armed, monotonic,
drill-recorded, and self-ref guards. Those checks must be green before this
candidate is handed to the panel.

## Deviations

No candidate-ref deviation arises: this record stages no scratch caller, so
nothing needs to resolve `CEREMONY_SELF_REF: "0.6.1"` before the tag exists.
