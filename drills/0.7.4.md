# 0.7.4 — drill record

Written 2026-08-16 by `cndgrr` against the 0.7.4 release PR (Refs #446),
candidate branch `build/446-cut-0-7-4` on `heavy-duty/ceremony`, cut from
`main` at `f657c06` — the merge of PR #445.

No disposable-repo rehearsal was executed for this cut and none is owed; this
file is a scope ruling, not an instrument emission. `drill/rehearsal.sh` was
not invoked, no scratch repo was created, and there is therefore no scratch
repo to name and no disposal to observe.

## Scope ruling — doors unchanged, no disposable-repo rehearsal

The three conditions in [drills/README.md](README.md#doors-unchanged) hold at
this candidate head, and each was measured here rather than copied from an
earlier record or from the authorizing issue (#233).

**1. The release-path diff is the pin line and nothing else.** The baseline is
`0.7.3`, and the measurement over the release path, taken at candidate
`2764399` — the commit that carries the stamp, the assembled notes and the
cleared markers — is:

```
$ git diff --numstat 0.7.3 -- <release-path>
1	1	.github/workflows/release.yml

$ git diff -U0 0.7.3 -- <release-path> | grep -E '^[-+]' | grep -vE '^(\+\+\+|---)'
-  CEREMONY_SELF_REF: "0.7.3"
+  CEREMONY_SELF_REF: "0.7.4"
```

One file, one line, and that line is the `CEREMONY_SELF_REF` pin the condition
exempts by name. No door logic, no decide table, no facts gathering, no
changelog extractor and no `bin/` script differs from the last rehearsed tag.

This record's own commit adds `drills/0.7.4.md` and nothing else, and
`drills/` is not on the release path, so the reading above is the reading at
the head that carries this file too. Re-run both commands to confirm it; that
is what they are printed for.

Measured at the branch point, `main` at `f657c06` — before this PR's own stamp
— the same diff is **empty**: `git diff --numstat 0.7.3 -- <release-path>`
prints nothing and the `-U0` line count is `0`. Nothing on the release path
had moved since the `0.7.3` tag at all, and this candidate's entire
release-path delta is the pin stamp it made itself.

**2. The release path came from the script, not from transcription.**
`.github/scripts/release-path.sh` returned six entries at this head, and the
diff above was taken over exactly them:

```
.github/workflows/release.yml
bin/
lib/version.sh
lib/decide.sh
lib/facts.sh
lib/changelog.sh
```

**3. The baseline is the last rehearsed tag, and it qualifies.**
[drills/0.7.3.md](0.7.3.md) is a **full rehearsal** — it carries the
instrument's own run sentence as its third line, naming
`build/413-release-0-7-3` and canonical candidate SHA `3ba3416`, and all
**eight** probes ran, eight rows in its table. The `0.7.3` release is
published (`2026-08-15T09:04:38Z`, `draft=false`, `prerelease=false`,
target `main`), and `main` was re-armed to `0.7.4-dev` afterwards at
`97f91a0` (`2026-08-15T09:04:39Z`, "chore: bump main to
0.7.4-dev — re-arm after publishing 0.7.3", a one-line `VERSION` change).

Here the last **rehearsed** tag is also the **previous** tag, which is a
coincidence of this cut rather than the rule: the anchor is the last
rehearsed tag (#233), and because this record is itself doors-unchanged it
anchors nothing — `0.7.3` remains the baseline for `0.7.5`.

**4. The four `CEREMONY_SELF_REF` carriers, three of them off the release
path.** The pin check requires every workflow carrying the env to name one
release and reds the tree otherwise, so this cut stamps four files while the
measurement above sees one. At this candidate head the carriers are
`.github/workflows/release.yml:141` (the one on the release path, and the
line the diff shows), `.github/workflows/ci-rerun.yml:43`,
`.github/workflows/labels.yml:58` and `.github/workflows/labels-sweep.yml:59`.
They are named here so the candidate's full diff and this record cannot be
read as disagreeing — `drills/0.6.3.md`'s own move, with one more carrier than
it had.

`ci-rerun.yml` is **new since `0.7.3`**: it did not exist when that tag was
cut, so the three carriers the `0.7.3` cut stamped are history and not a
checklist. Stamping three of four reds `.github/scripts/self-ref-check.sh` at
this head, which is the guard doing its job (#351).

**A waiver was considered and is the weaker record.** `WAIVED` is a
maintainer's judgement under the standing paragraph; `doors unchanged` is a
mechanically checked claim the panel can re-run from this file. Where the
conditions hold, the checkable shape is the one owed — the narrower shape is
not a substitute for a rehearsal or a waiver, but it is not optional in
favour of them either.

## Standing evidence at the candidate head

| # | probe | where | result |
|---|---|---|---|
| 1 | decide + merge-door step-replay (dogfood) | `release-exercise / step-replay (dogfood)` on this PR | CI-gating; green required to merge |
| 2 | decide + merge-door step-replay (consumer) | `release-exercise / step-replay (consumer)` on this PR | CI-gating; green required to merge |
| 3 | fragment chain: armed → assemble → monotonic | `release-exercise / fixture-chain` on this PR | CI-gating; green required to merge |
| 4 | the real tree's own guards (armed, monotonic, assembled, drill-recorded, runner-isolated, sha-pinned) | `self-guards` on this PR | CI-gating; green required to merge |
| 5 | the drill record's round trip, classifying this file rather than grading it | `self-guards / Drill record round trip` on this PR | CI-gating; green required to merge |
| 6 | the self-ref pin: every workflow names one ceremony release | `test / Self-ref pin` on this PR | CI-gating; green required to merge |
| 7 | the documentation availability markers against the assembled section | `test / Documentation availability markers` on this PR | CI-gating; green required to merge |
| 8 | the contract suite, shellcheck and actionlint | `test / Tests`, `test / Shellcheck`, `test / Actionlint` on this PR | CI-gating; green required to merge |

Probe 6 covers the pin measurement this record's fourth block is about: a
stale or partial `CEREMONY_SELF_REF` fails ceremony's own CI here, so all four
stamps are verified by the same run that verifies everything else. Probe 5 is
the one to read rather than tick — its log says which branch it took, and the
branch this record expects is the one that classifies a scope ruling and
declines the round trip (#373 D9).

## Deviations

None. This record stages no scratch caller, so nothing needs to resolve
`CEREMONY_SELF_REF: "0.7.4"` before the tag exists — the candidate-ref
deviation that shape would raise does not arise.

The payload is twelve fragments: #418, #419, #420, #423, #424, #425, #426,
#429, #430, #437, #440 and #441. They land in the doctrine files, in
`actions/issueflow-reconcile/` and `lib/checks.sh`, in `actions/ci-rerun/`
and the `.github/workflows/ci-rerun.yml` caller, in `.github/labels.conf`,
and in `docs/CONSUMERS.md`. None of it is on the release path and no release
door reads any of it: the new workflow is a labels-side servicing path, the
reconciler and its libs are consumed by the labels workflows, and the
doctrine files are bytes a consumer re-vendors at its next pin bump. No
release-workflow input, no new release guard, and no change to any door a
drill would probe.

PR #445 (#444) also sits in this cut's tree while shipping no changelog
fragment, and both facts are correct at once: it repaired two prose
statements about the headless-`idle` carrier set and added the fixture that
makes them falsifiable, publishing nothing a consumer can observe.
`changelog-armed` proves the directory is armed and every fragment
publishable, never that each PR added one.

One sentence this cut deliberately leaves wrong is recorded here rather than
fixed: `docs/CONSUMERS.md` says the sweep diagnoses *three* board shapes, and
this tag ships four. #441 merged before #440's `stalled` family landed and
could not have said otherwise. This cut clears that line's availability marker
and changes nothing else on it; correcting the count is a follow-up triage
owns (#446 D8).
