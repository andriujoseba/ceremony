# 0.6.0 — drill record

Run in progress on 2026-08-04 by `andriujoseba` for the release candidate
branch `build/249-release-0-6-0` (Refs #249).

## Scope measurement — full rehearsal required

The last fully rehearsed tag is `0.4.0`. At this branch's starting base,
`.github/scripts/release-path.sh` names `.github/workflows/release.yml`,
`bin/`, `lib/version.sh`, `lib/decide.sh`, `lib/facts.sh`, and
`lib/changelog.sh`. The measured `git diff 0.4.0..HEAD -- <release-path>`
includes changes to `lib/changelog.sh`, so the doors-unchanged exception does
not apply. This release therefore owes the full disposable-repository
rehearsal described by `drills/README.md`.

No probe result or disposal state is claimed yet. This draft record will be
replaced with the observed repository, candidate pin, six probe results, and
the observed archive state after the rehearsal completes.
