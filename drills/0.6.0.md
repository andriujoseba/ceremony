# 0.6.0 — drill record

Run 2026-08-04 by `andriujoseba` against release PR #305 candidate machinery
at `b10c6211e6f7d01514ac24585c2bdfab32f7a546` (Refs #249).

Where: disposable private repo
`andriujoseba/ceremony-drill-0.6.0-305`, carrying the
`docs/CONSUMERS.md` release caller and a fragment-mode fixture armed at
`0.6.0-dev`. The fixture had `changelog.d/README.md`, one release fragment,
and a non-blank drill record. The repository is archived, pending the
operator's delete.

## Why a full rehearsal was required

The last fully rehearsed tag is `0.4.0`. At the candidate machinery commit,
`.github/scripts/release-path.sh` named `.github/workflows/release.yml`,
`bin/`, `lib/version.sh`, `lib/decide.sh`, `lib/facts.sh`, and
`lib/changelog.sh`. The measured `git diff 0.4.0..b10c621 -- <release-path>`
included changes to `lib/changelog.sh`, so the doors-unchanged exception did
not apply.

## Candidate-ref deviation

The pure consumer path cannot resolve `CEREMONY_SELF_REF: "0.6.0"` before
the candidate creates that tag. No `0.6.0` branch was created in
`heavy-duty/ceremony`. The scratch caller instead used
`andriujoseba/ceremony@drill/0.6.0`, whose parent is the candidate machinery
commit `b10c6211e6f7d01514ac24585c2bdfab32f7a546` and whose only additional
commit, `113b331`, rewrites all three `CEREMONY_SELF_REF` carriers to that
same canonical candidate SHA. All runtime machinery was therefore fetched
from the 0.6.0 candidate tree.

## Probes

| # | probe | run | result |
|---|---|---|---|
| 1 | merge-door ceremony | 30932346569 (attempt 1) | ✅ one `0.6.0` release; tag and PR #3 merge commit both `d93a10d112626a99949043e10067904697905b26`; main re-armed at `eadc7d63a298357381ee770a0b2c9f2e4e55b2a2` to `0.6.1-dev` with only `changelog.d/README.md` |
| 2 | mislabeled ordinary PR | 30932182149 | ✅ green NOTICE no-op; no tag or release |
| 3 | bare-version PR without `release` | 30932244202 | ✅ refused at decide; no tag or release; restoration run 30932286637 was a green no-op |
| 4 | re-run completed ceremony | 30932346569 (attempt 2) | ✅ refused because the tag/release already existed; the `0.6.0` release count stayed one |
| 5 | manual matching tag | 30932483401 | ✅ `0.7.0` published with its changelog section; main stayed at `eadc7d63a298357381ee770a0b2c9f2e4e55b2a2` |
| 6 | mismatched tag | 30932534814 | ✅ refused before publication; no `9.9.9` release, and the probe tag was removed afterward |

The merge-door `0.6.0` release body was exactly:

```text
- The 0.6.0 drill exercises fragment-mode release assembly (#1).
```

The matching tag-door `0.7.0` release body was exactly:

```text
- The tag-door drill publishes the matching 0.7.0 tree (#2).
```

## Setup

The armed fixture was committed before the caller, and installing the caller
produced green baseline run 30932131599. After the unlabeled bare-version
refusal, its merge commit was reverted to restore the armed fixture; green
run 30932286637 proved that restoration before the merge-door probe began.
