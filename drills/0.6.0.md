# 0.6.0 — drill record

Run 2026-08-04 by `andriujoseba` against the release candidate branch
`build/249-release-0-6-0`, built from `heavy-duty/ceremony` main at
`d30b091` (Refs #249).

## Scope ruling — doors unchanged, no disposable-repo rehearsal

The 0.4.0 drill (`drills/0.4.0.md`) rehearsed both release doors end to end
in a disposable repository. Measured afresh for this candidate against that
last rehearsed tag, all three doors-unchanged conditions in
`drills/README.md` hold:

1. `.github/scripts/release-path.sh` prints exactly
   `.github/workflows/release.yml`, `bin/`, `lib/version.sh`,
   `lib/decide.sh`, `lib/facts.sh`, and `lib/changelog.sh`.
2. `git diff 0.4.0..HEAD -- <those paths>` changes only the
   `CEREMONY_SELF_REF` pin in `.github/workflows/release.yml`, from `0.4.0`
   to this candidate's `0.6.0`. No release-door logic, decision table,
   publish step, or release library changed.
3. `0.4.0` has a full rehearsal record and a published GitHub release, and
   main is re-armed at `0.5.1-dev` before this candidate stamps bare `0.6.0`.

The 0.4.1 and 0.5.0 releases also exercised the merge door in production
after that rehearsal. A new disposable-repo run would therefore execute the
same release-path bytes. The panel reviews this claim like any other evidence;
if any reviewer rules that a full rehearsal is owed, that verdict wins.

## Standing evidence at the candidate head

| # | probe | where | result |
|---|---|---|---|
| 1 | decide + merge-door step replay (dogfood) | `release-exercise / step-replay (dogfood)` | CI-gating; green required before the round signal |
| 2 | decide + merge-door step replay (consumer) | `release-exercise / step-replay (consumer)` | CI-gating; green required before the round signal |
| 3 | fragment chain: armed → assemble → monotonic | `release-exercise / fixture-chain` | CI-gating; green required before the round signal |
| 4 | candidate tree guards: armed, monotonic, assembled, drill-recorded, self-ref, marker check | `self-guards` | CI-gating; green required before the round signal |

## Deviations

No candidate-ref deviation arises: this record stages no scratch caller, so
nothing needs to resolve `CEREMONY_SELF_REF: "0.6.0"` before the tag exists.
The post-tag consumer proof belongs to crew's 0.1.2 integration member and is
reported back on discussion #223 by triage, as #249 requires.
